<?xml version="1.0" encoding="UTF-8"?>
<!-- $Id$ -->

<project name="scala" default="build">

    <description>
      Legacy Ant entry point delegating to the make-based bootstrap build.
      The Java 8 bootstrap pipeline is defined in the Makefiles and is the
      single supported build graph.
    </description>

    <property environment="env"/>

    <target name="build" description="Builds the bootstrap compiler, library and tools.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="all"/>
        </exec>
    </target>

    <target name="boot" description="Builds the bootstrap compiler.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="boot.all"/>
        </exec>
    </target>

    <target name="main" description="Builds the main compiler, library and tools.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="main.all"/>
        </exec>
    </target>

    <target name="test" description="Builds the test compiler and libraries.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="test.all"/>
        </exec>
    </target>

    <target name="jars" description="Builds the library and tools jars.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="jars"/>
        </exec>
    </target>

    <target name="clean" description="Removes main build products.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="clean"/>
        </exec>
    </target>

    <target name="distclean" description="Removes all generated products and markers.">
        <exec executable="make" dir="${basedir}" failonerror="true">
            <arg value="distclean"/>
        </exec>
    </target>

    <target name="all" depends="clean,build"
            description="Rebuilds everything from a clean workspace."/>

</project>
