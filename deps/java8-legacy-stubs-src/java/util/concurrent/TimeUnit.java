package java.util.concurrent;

public enum TimeUnit {
  NANOSECONDS, MICROSECONDS, MILLISECONDS, SECONDS, MINUTES, HOURS, DAYS;
  public long convert(long sourceDuration, TimeUnit sourceUnit) { return 0L; }
  public long toNanos(long duration) { return 0L; }
  public long toMicros(long duration) { return 0L; }
  public long toMillis(long duration) { return 0L; }
  public long toSeconds(long duration) { return 0L; }
  public long toMinutes(long duration) { return 0L; }
  public long toHours(long duration) { return 0L; }
  public long toDays(long duration) { return 0L; }
}
